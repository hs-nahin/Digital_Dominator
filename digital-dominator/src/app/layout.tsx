import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Digital Dominator - Personal Learning Tracker',
  description: 'Track your learning progress with modules and tasks. Stay organized and achieve your learning goals.',
  keywords: 'learning, tracker, education, progress, tasks, modules',
  authors: [{ name: 'Digital Dominator' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}